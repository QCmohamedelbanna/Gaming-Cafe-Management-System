<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Human-readable console output for local development. -->
    <springProfile name="dev">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n</pattern>
            </encoder>
        </appender>
    </springProfile>

    <!-- Structured JSON console output for production, so log shippers
         (CloudWatch, Loki, ELK, etc.) can parse fields without a custom
         grok pattern. -->
    <springProfile name="prod">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <customFields>{"app":"playstation-cafe-backend"}</customFields>
            </encoder>
        </appender>
    </springProfile>

    <!-- Dedicated audit trail: one JSON line per sensitive action (login,
         checkout, refund, cancel, product delete, discount). Always JSON,
         regardless of profile, and kept out of the app's regular log noise
         via additivity=false. Rotated daily, kept for a year. -->
    <appender name="AUDIT" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/audit.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/audit-%d{yyyy-MM-dd}.log.gz</fileNamePattern>
            <maxHistory>365</maxHistory>
        </rollingPolicy>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <customFields>{"app":"playstation-cafe-backend","stream":"audit"}</customFields>
        </encoder>
    </appender>

    <logger name="AUDIT" level="INFO" additivity="false">
        <appender-ref ref="AUDIT"/>
    </logger>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>
